use anyhow::Result;

/// Serve as an MCP server via stdio.
///
/// Ref: src/entrypoints/mcp.ts
pub async fn serve() -> Result<()> {
    // TODO: initialize MCP server, listen on stdio, dispatch tool calls
    Ok(())
}
